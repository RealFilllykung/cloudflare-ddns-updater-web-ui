version: '3.8'

services:
  frontend:
    build: ./frontend
    container_name: frontend
    networks:
      - ddns-updater
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - NODE_ENV=production

  backend:
    build: ./backend
    container_name: backend
    networks:
      - ddns-updater
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_HOST=database
      - DB_PORT=5432
      - DB_NAME=ddns
      - DB_USER=ddnsuser
      - DB_PASSWORD=ddnspassword
    depends_on:
      - database

  database:
    image: postgres:16
    container_name: database
    networks:
      - ddns-updater
    environment:
      - POSTGRES_DB=ddns
      - POSTGRES_USER=ddnsuser
      - POSTGRES_PASSWORD=ddnspassword
    volumes:
      - db_data:/var/lib/postgresql/data

networks:
  ddns-updater:
    driver: bridge

volumes:
  db_data:
